"""The single v0.3.1 structured-contract identifier.

``contract_version`` labels affected results, citations, contexts, and traces. It is
distinct from ``index_version`` (the index projection version) and must never be conflated
with it.
"""
from __future__ import annotations

CONTRACT_VERSION = "jarvis.query.v0.3.1"
INDEX_VERSION = "jarvis.index.v0.3.1"
